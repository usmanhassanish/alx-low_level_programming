#include "dog.h"
/**
 * print_dog - prints the elements of dog
 * @d: dog structure
 */
void print_dog(struct dog *d)
{
  if (d == NULL)
  {
    
  }
  else
  {
    if (d.name = NULL)
      printf("Name: (nil)")
    if (d.age = NULL)
      printf("Age: (nil)")
    if (d.owmer = NULL)
      printf("Owner: (nil)")
    
    printf("%s", d.name);
    printf("%f", d.age);
    printf("%s", d.owner);
  }
}
