#include "main.h"
/**
 * main - the entry point of the program
 * @argc: argument count
 * @argv: array of argumensts
 * Return: returns 0
 */
int main(__attribute__((unused)) int argc, char **argv)
{
	printf("%s\n", argv[0]);
	return (0);
}
