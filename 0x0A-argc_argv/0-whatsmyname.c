#include <stdio.h>
/**
 * main - entry point
 * @argc: counts number of arguments in main
 * @argv: array that holds arguments in main
 * Return: returns 0 on sucess
 */
int main(int argc, char **argv)
{
	printf("%s\n", argv[0]);
	return (0);
}
